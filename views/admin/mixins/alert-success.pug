mixin alert-success(time)
    - if (messages.success)
        .message.success 
            div(
                show-alert
                class="alert alert-success"
                data-time=time
            ) #{messages.success}    
                button(
                    close-alert
                ) X