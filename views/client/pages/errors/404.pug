doctype html
html
    head
        title Lỗi 404
        style.
            body {
                font-family: Arial, sans-serif;
                margin: 0;
                padding: 0;
                display: flex;
                justify-content: center;
                align-items: center;
                height: 100vh;
                background-color: #f8f9fa;
                color: #333;
            }

            .error-container {
                text-align: center;
            }

            h1 {
                font-size: 3rem;
                color: #ff6b6b;
                margin-bottom: 10px;
            }

            p {
                font-size: 1rem;
                margin-bottom: 20px;
            }

            button {
                padding: 10px 20px;
                font-size: 1rem;
                color: #fff;
                background-color: #007bff;
                border: none;
                border-radius: 5px;
                cursor: pointer;
                transition: background-color 0.3s ease, transform 0.2s ease;
            }

            button:hover {
                background-color: #0056b3;
                transform: scale(1.05);
            }

            button:active {
                transform: scale(0.95);
            }
    body
        .error-container
            h1 Lỗi 404 - Trang không tồn tại
            p Xin lỗi, trang bạn đang tìm kiếm không tồn tại hoặc đã bị xóa.
            button(onclick="history.back()") Trở lại
